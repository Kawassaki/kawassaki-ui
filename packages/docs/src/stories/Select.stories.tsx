import type { Meta, StoryObj } from '@storybook/react'
import { Select, SelectProps } from '@kawassaki-ui/react'

export default {
  title: 'Form/Select',
  component: Select,
  args: {},
} as Meta<SelectProps>

export const Primary: StoryObj<SelectProps> = {}
